// Variáveis de ambiente
// - Não vão para o Github
// - Feito em Gitpod / Settings
// Variáveis devem ser criadas com letras em caixa alta

// eval $(gp env -e)
// echo $DB_NAME 


// npm start inicia o site

// npm install ou npm i

// no padrão do Angular, Node, Express e MongoDB usa-se letra maiúscula e termo no singular

// try / catch servem para que quando acontecer um erro (por qualquer motivo) ele retorne
// um erro ao usuário. O sistema sempre tenta executar o try e, se houver algum erro,
// o catch entra em ação e informa o erro na tela.

// Na pasta models, os arquivos tem a primeira letra maiúscula.